
CHECK! 1. Animationen eingliedern (nur naiver Versuch)
	
CHECK! 2. Zoomen besser verstehen können
CHECK! 3. Zoomen eingliedern (nur naiver Versuch) - funktioniert parallel zu Animationen

CHECK! 4. Zoomen
weiteres: 
	- Button zum Zurücksetzen des Zooms (erst mit dem "TerminateButton" testen)
	- Zoom relativ zur Maus ermöglichen
		- Idee: Anpassung des Pivotpoints darf NICHT die scene oder stage x koordinaten haben, sondern die in dem fenster UND das fenster muss eine feste größe (zb 600x600) haben
	
CHECK! 5. Dragging
weiteres:
	- Button zum Zurücksetzen der Translation

CHECK! 6. Animationen
	CHECK! - Complete Visualization, aufeinander warten
		CHECK! - aufeinander warten => animation.setDelay(Duration.millies(3000))
		CHECK! - Lösungsansätze:
			Idee1: anfangs auf unsichtbar stellen -> Problem: macht alles nochmal umfangreicher
			Idee2: was an der Ausführung der complete visualization ändern. Derzeit: anfangs ganz durchgehen, dann schritt für schritt mit den animationen. Vielleicht besser: tatsächlich schritt für schritt. Mit timer? Eher nicht. Mit atEnd Event der Animation den nächsten Schritt auslösen?
				=> Idee2 mit onFinishedEvents der Animationen klappt gut
	CHECK! - was bei zu schnellen Eingaben?
		CHECK! - Gewünscht: Momentane Position wird auf Zielposition gesetzt, also Animation bricht ab 
			- => animation.jumpTo("end")
				- Idee: bei jedem Befehlsaufruf in ExperimentVisualization
		CHECK! - Lösungsvorschläge:
			Idee 1: statt setTranslate eine Animation mit Zeit 0 erstellen dafür
            Idee 2: die translation als atEnd Event (oder wie auch immer) auf 0 setzen
				=> Idee 1 klappt anscheinend
	CHECK! - Beta-Animationen (nur testen ob es grundsätzlich geht)
		CHECK! - create, insert
		CHECK! - swap (schwieriger)
	CHECK! - complete visualization auch bei noch nicht implementierten animationen anwenden können
		CHECK! - durch Überladung von generateNode() und dabei erstellen von nullTransition
		CHECK! - Get in zb ArrayVisualization funktioniert hier auch
weiteres:
	- deleteExperiment klappt nicht (wahrscheinlich auch deleteElement nicht), da die Sachen zuerst gelöscht werden, bevor die Animationen durchlaufen können
		- Lösungsvorschlag:
			Idee 1: das tatsächliche Löschen als atEnd Event (oder wie auch immer) der Animation aufrufen
			Idee 2: irgendwie ein Event bauen, das beim Ende der Animation aktiviert wird und dabei das Element löscht
				- Problem bei beiden: könnte leicht bei zu schnellen Eingaben zu Fehlern kommen. Vielleicht kann ich es wirklich rein visuell halten?
	- animation für "get" funktionen erstellen
	- Complete Visualization
		- Hervorhebung in der Befehlsliste geht von Befehl zu Befehl mit
			- indem die derzeitige Hervorhebung in dem Modus ausgeblendet und eine andere verwendet wird?
		- Button um direkt zum Endergebnis zu springen
		- Button um Pause zu drücken
	

CHECK! (obsolet) 7. "Timer"? Animationen nacheinander abspielbar? -> Complete Visualization

8. "Datenstruktur: Baum" Kapitel schreiben
	- Zunächst Plan machen, also Unterkapitel was geschrieben muss und in welcher Reihenfolge


11. ScrollPane entfernen und stattdessen anderen Knoten, zb normales Pane, welcher aber nur die feste Größe als Fenster anzeigt

12. Eine "config" Datei erstellen, für bessere Wartbarkeit und individuelle Nutzung (siehe AllgemeineTDL)
	- https://stackoverflow.com/questions/16273174/how-to-read-a-configuration-file-in-java
	- Variablen:
		- fieldDistance und standardDuration (AbstractAnimationCreator)
		- filepaths (zwei Stellen, weiß nicht mehr wo)
		- Fenstergöße und full window (ParentViewModel)

21. Refactoring
